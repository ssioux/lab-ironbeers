<div>

    <h1>Cervezas</h1>
    
    {{#each beersFromApi}}

    <img src="{{this.image_url}}" alt="beer" width="70px">
    <p>{{this.name}}</p> 
    <p>{{this.description}} 
    <p>{{this.tagline}}</p>
    
  

    {{/each}}

</div>